# .github/workflows/pr-workflow.yml
name: Pull Request Workflow

on:
  pull_request:
    branches:
      - master  # or your target branch (e.g., 'develop')

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: echo "Running tests for PR..."
